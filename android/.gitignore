gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# استثنا: این کیستورِ دیباگِ عمداً چک‌شده‌ست (برای امضای یکسان تو
# همه‌ی بیلدهای CI و لوکال — بخشِ کامنتِ signingConfigs تو
# app/build.gradle.kts رو ببین). فقط دیباگه، حساس نیست؛ کیستورِ
# release هیچ‌وقت نباید اینجوری اضافه بشه.
!app/debug.keystore
